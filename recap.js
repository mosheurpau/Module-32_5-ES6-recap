let price = 50;
price = 100;
const name = 'Mosheur';
const myTmplate = `This is template string ${name}. 
this is new.
`;
// console.log(myTmplate);

const persons = {
    name: 'mehedi',
    age: 24,
    hobby: 'biking'
};

// console.log(`My name is ${persons.name}. 
// I'm ${persons.age} years old.
// My hobby is ${persons.hobby}`);

const numberDivision = num => num / 5;
// console.log(numberDivision(20));

const twoParamear = (num1, num2) => {
    const sum = num1 + num2;
    const newNum1 = num1 * sum;
    const newNum2 = num2 * sum;
    console.log(newNum1 + ' ' + newNum2);
}
twoParamear(4, 6);

const threeParametar = (num1, num2, num3) => {
    const mult = num1 * num2 * num3;
    // console.log(mult);
}
threeParametar(2, 4, 2);

const threeParametars = (num1, num2, num3) => num1 * num2 * num3;
console.log(threeParametars(2, 4, 3));
const myNumbers = [3, 4, 5, 6, 7, 8, 9];
// const myNewNumber = myNumbers.map(n => n * 5);
// console.log(myNewNumber);

const odd = myNumbers.filter(n => n % 2 != 0)
// console.log(odd);

const getfish = [{ name: 'Elish', age: 24, price: 3000 },
{ name: 'frist', age: 24, price: 4000 },
{ name: 'second', age: 24, price: 8000 },
{ name: 'three', age: 24, price: 9000 },
{ name: 'four', age: 24, price: 4000 }
];
/* 
const result = getfish.find(fish => fish.price > 5000);
console.log(result); */

const [frist, second, three, four] = getfish;
console.log(three);

const janpakhi = {
    voice: 'babe',
    attitude: 'high',
    isCute: true
}

const { voice, attitude, isCute } = janpakhi;
// console.log(voice, isCute);

const getSum = (num1, num2, num3 = 7) => num1 + num2 + num3;
console.log(getSum(5, 5, 5));


































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































